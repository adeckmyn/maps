import("graphics")
importFrom("utils", "read.delim")
export(world.legacy,ISOexpand,ISOalpha,area.map, identify.map, map, map.axes, map.cities, map.scale, map.text, map.where, map.wrap, match.map, smooth.map, .map.range)
S3method(identify, map)
